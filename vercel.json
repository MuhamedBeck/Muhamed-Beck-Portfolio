{
  "$schema": "https://openapi.vercel.sh/vercel.json",

  "//": [
    "The site now runs on Cloudflare Workers (see wrangler.jsonc).",
    "This file only keeps the old mbeck.vercel.app deployment alive as a",
    "permanent redirect, so existing links and bookmarks reach the new domain",
    "and search engines consolidate the two addresses instead of treating them",
    "as duplicates. Delete the Vercel project and this file once Search Console",
    "shows no traffic on the old host."
  ],

  "redirects": [
    {
      "source": "/:path*",
      "destination": "https://muhamedbeck.com/:path*",
      "permanent": true
    }
  ]
}
